import type { SidebarProps } from '@/types'
import { FiltersPannelClientWrapper } from './client'

export function FiltersPannel1(props: SidebarProps) {
  return <FiltersPannelClientWrapper {...props} />
}
